---
import BaseLayout from "../layouts/BaseLayout.astro";
import CmsPageRenderer from "../components/CmsPageRenderer.astro";
import { getPage } from "@/lib/posts";

const page = await getPage("/contact");

const pageTitle = page?.seo?.title || page?.title || "Contact | ShaunEvans.com";
const description =
  page?.seo?.meta_description ||
  "Contact Shaun Evans - Canadian-Australian Saxophonist, Arranger & Musical Director";
const site_name = "ShaunEvans.com";
---

<BaseLayout
  title={pageTitle}
  description={description}
  site_name={site_name}
  is404Page={false}
  hideHeaderFooter={false}
>
  {
    page ? (
      <CmsPageRenderer blocks={page.blocks} />
    ) : (
      <div class="mx-auto max-w-3xl px-4 py-8">
        <p>Contact page is not available right now. Please email shaun@shaunevans.com.</p>
      </div>
    )
  }
</BaseLayout>
